# Security

Use generated upload filenames outside public assets, validate PDF/DOCX/TXT size and MIME/signature, hash passwords with Argon2, require authorization per user record, and redact secrets and sensitive answers from logs. The trigger uses a bearer secret.

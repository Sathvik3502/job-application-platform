# Local development

Use Docker Compose for PostgreSQL and Redis. `MOCK_MODE=true`, `DRY_RUN=true`, and `AUTO_APPLY_ENABLED=false` are default safety settings. Install Playwright browsers with `npx playwright install` when implementing browser tests.

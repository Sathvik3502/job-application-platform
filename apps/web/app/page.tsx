import Link from "next/link";
export default function Home(){const mockMode=process.env.MOCK_MODE!=="false";return <section><h1>A safer way to apply at scale.</h1><p className="muted">Discover, score, tailor, and review applications with local-first AI and explicit submission safety gates.</p><p><Link className="button" href="/dashboard">{mockMode?"Open demo dashboard":"Open dashboard"}</Link></p><div className="grid stats"><article className="card"><h2>Local-first</h2><p className="muted">Ollama, Postgres, Redis and Playwright—no paid API is required.</p></article><article className="card"><h2>Human control</h2><p className="muted">Ambiguous questions, CAPTCHA, and unsupported sites always pause for review.</p></article><article className="card"><h2>Traceable matching</h2><p className="muted">Every score includes deterministic sub-scores and profile evidence.</p></article></div></section>}

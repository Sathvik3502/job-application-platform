import Link from "next/link";import {demoJob,demoProfile} from "@/lib/demo";import {scoreJob,eligibility} from "@job-platform/matching";
export default function Jobs(){const score=scoreJob(demoProfile,demoJob), gate=eligibility(demoProfile,demoJob,score);return <><h1>Job discovery</h1><p className="muted">Mock connector results. Production connectors must respect each source’s terms and access controls.</p><article className="card"><div className="row"><div><h2>{demoJob.title}</h2><p className="muted">{demoJob.company} · {demoJob.location} · {demoJob.source}</p></div><div><div className="stat good">{score.overall}%</div><span className="badge">{gate.status}</span></div></div><p>{demoJob.description}</p><p className="muted">Matched: {score.matchedSkills.join(", ")}</p><Link className="button" href="/jobs/job_01">View analysis</Link></article></>}

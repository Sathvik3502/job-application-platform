import Link from "next/link";import {getSession} from "@/lib/auth";import {prisma} from "@/lib/prisma";
export default async function Applications(){const session=await getSession();if(!session)return <><h1>Applications</h1><p className="muted">Sign in to see your private application history.</p><Link className="button" href="/login">Sign in</Link></>;const applications=await prisma.application.findMany({where:{userId:session.userId},include:{job:true},orderBy:{updatedAt:"desc"}});return <><h1>Applications</h1><article className="card">{applications.length===0?<p className="muted">Your review queue is empty.</p>:<table className="table"><thead><tr><th>Company</th><th>Role</th><th>Score</th><th>Result</th></tr></thead><tbody>{applications.map(application=><tr key={application.id}><td>{application.job.company}</td><td>{application.job.title}</td><td>{application.matchScore}%</td><td><span className="badge review">{application.status}</span></td></tr>)}</tbody></table>}</article></>}
